@{
    ViewData["Title"] = "Báo cáo bán hàng";
}

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="~/js/reports.js"></script>
}

<div class="min-h-screen bg-gray-50">
    <!-- Header -->
    @await Component.InvokeAsync("PageHeader", new { title = "Báo cáo bán hàng", showTabs = false })

    <!-- KPI Cards -->
    <div class="p-6">
        <div class="grid grid-cols-5 gap-4 mb-6">
            <div class="bg-white rounded-lg shadow-sm p-4 border-l-4 border-blue-500">
                <div class="text-sm text-gray-600 mb-1">Doanh thu thuần</div>
                <div class="text-2xl font-bold text-gray-900" id="totalRevenue">0</div>
            </div>
            <div class="bg-white rounded-lg shadow-sm p-4 border-l-4 border-teal-500">
                <div class="text-sm text-gray-600 mb-1">Đã thu</div>
                <div class="text-2xl font-bold text-gray-900" id="totalReceived">0</div>
            </div>
            <div class="bg-white rounded-lg shadow-sm p-4 border-l-4 border-purple-500">
                <div class="text-sm text-gray-600 mb-1">Thực nhận</div>
                <div class="text-2xl font-bold text-gray-900" id="actualReceived">0</div>
            </div>
            <div class="bg-white rounded-lg shadow-sm p-4 border-l-4 border-orange-500">
                <div class="text-sm text-gray-600 mb-1">Đơn hàng</div>
                <div class="text-2xl font-bold text-gray-900" id="totalOrders">0</div>
            </div>
            <div class="bg-white rounded-lg shadow-sm p-4 border-l-4 border-gray-400">
                <div class="text-sm text-gray-600 mb-1">Sản phẩm</div>
                <div class="text-2xl font-bold text-gray-900" id="totalProducts">0</div>
            </div>
        </div>
        <div class="bg-white rounded-lg shadow-sm mb-6">

            <div class="bg-white border-b border-gray-200 px-6 py-4">
                <div class="flex gap-4 items-end">
                    <div>
                        <label class="block text-xs font-medium text-gray-600 mb-1">Từ ngày</label>
                        <input type="date"
                               id="fromDate"
                               class="px-3 py-2.5 text-sm border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                    </div>
                    <div>
                        <label class="block text-xs font-medium text-gray-600 mb-1">Đến ngày</label>
                        <input type="date"
                               id="toDate"
                               class="px-3 py-2.5 text-sm border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                    </div>
                    <div>
                        <label class="block text-xs font-medium text-gray-600 mb-1">Nhóm theo</label>
                        <select id="groupBy" class="px-3 py-2.5 text-sm border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                            <option value="day">Ngày</option>
                            <option value="month">Tháng</option>
                            <option value="year">Năm</option>
                        </select>
                    </div>
                    <button onclick="loadReport()" class="px-6 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 text-sm">
                        <i class="fas fa-sync mr-2"></i>Cập nhật
                    </button>
                </div>
            </div>

            <!-- Chart Section -->
            <div class="grid grid-cols-12 gap-6 p-6">
                <!-- Main Chart -->
                <div class="col-span-8">
                    <div class="mb-4">
                        <h3 class="text-lg font-bold text-gray-900">Biểu đồ</h3>
                    </div>
                    <div class="relative" style="height: 400px;">
                        <canvas id="salesChart"></canvas>
                    </div>
                    <!-- Legend -->
                    <div class="flex items-center justify-center gap-6 mt-4">
                        <div class="flex items-center gap-2">
                            <div class="w-4 h-4 rounded bg-blue-500"></div>
                            <span class="text-sm text-gray-600">Doanh thu thuần</span>
                        </div>
                        <div class="flex items-center gap-2">
                            <div class="w-4 h-4 rounded bg-orange-500"></div>
                            <span class="text-sm text-gray-600">Đơn hàng</span>
                        </div>
                    </div>
                </div>

                <!-- Best Selling Products -->
                <div class="col-span-4">
                    <div class="mb-4">
                        <h3 class="text-lg font-bold text-gray-900">Sản phẩm bán chạy</h3>
                    </div>
                    <div class="space-y-4" id="topProductsList">
                        <div class="text-center py-8 text-gray-500">Đang tải...</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

